1. epochs 를 1000에서 500 으로 바꿈
2. batch_size 를 64에서 128로 바꿈 (한 epoch 에서 반복 횟수는 전체 데이터갯수/batch_size 로 나눈 값임)]
    -> batch_size 를 128로 하니까 연산량이 많아져 gpu 메모리가 연산을 시작하지 못함
    -> 100으로 바꾸어보니까 연산을 시작 함 
3. 50 epoch 마다 weights 폴더에 훈련된 가중치 저장
