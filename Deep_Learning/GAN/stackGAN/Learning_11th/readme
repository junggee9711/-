1. 10th 코드에서,
   cropped 된 이미지를 입력으로 넣으면(2만개, batch_size = 128, CelebA_dataset_reduce_cropped), 
--> 문제는, OpenCV 코드가 정면이 아닌 사진은 얼굴 검출을 하지 못하기 때문에 _cropped/img_align_celeba/images 폴더 안에 그러한 사진들은 제거 된후 저장
    (원본 데이터는 180978 개로, reduce 데이터는 17829 개로 줄어듬)
    그러므로, 이 데이터들과 파일명, 속성값들을 서로 매칭 시켜 훈련시켜야 함

2. CUB 의 훈련된 text_embedding.pickle 파일을 열어보니 값이 -1.- ~ 1.- 사이였다 (양수평균: 0.08805331063548615, 음수평균: -0.09171628504722455)
   그러므로, 얼굴 속성값 데이터는 0.1, -0.1 로 두어 입력으로 넣는다



